{
  "name": "yarapa-code-standard",
  "private": true,
  "description": "YARAPA monorepo for the eslint-config-yarapa public ESLint Flat Config package.",
  "type": "module",
  "scripts": {
    "build": "turbo run build",
    "changeset": "changeset",
    "changeset:status": "changeset status --since main",
    "check-types": "turbo run check-types",
    "dev": "turbo run dev",
    "format": "pnpm --filter eslint-config-yarapa build && eslint . --fix",
    "inspect": "pnpm --filter eslint-config-yarapa build && eslint-config-inspector",
    "knip": "knip",
    "lint": "pnpm --filter eslint-config-yarapa build && eslint .",
    "prepare": "husky",
    "test": "turbo run test",
    "verify": "pnpm --filter eslint-config-yarapa verify",
    "version-packages": "changeset version"
  },
  "devDependencies": {
    "@changesets/cli": "3.0.1",
    "@commitlint/cli": "21.2.2",
    "@commitlint/config-conventional": "21.2.2",
    "@eslint/config-inspector": "3.4.0",
    "eslint": "10.9.1",
    "husky": "9.1.7",
    "knip": "^6.33.0",
    "lint-staged": "17.4.1",
    "turbo": "^2.10.12",
    "typescript": "7.0.2"
  },
  "packageManager": "pnpm@11.23.0",
  "engines": {
    "node": ">=24.15.0"
  }
}
